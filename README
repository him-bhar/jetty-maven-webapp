This project contains an ideal case where you want a webapp to be deployed as an internal part of an app, rather than doing it on a separate container.
Right now only initial draft has been added to get a feel, more enhancements underway.
